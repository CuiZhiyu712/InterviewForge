import{$,api,escapeHtml}from"./common.js";try{const items=await api("/api/sources");$("#list").innerHTML=items.map((item)=>`<article class="card"><h2>${escapeHtml(item.name)}</h2><p><a target="_blank" rel="noopener" href="${item.url}">${escapeHtml(item.url)} ↗</a></p><div class="meta">提交 ${escapeHtml(item.commit_hash.slice(0,12))} · ${item.documents} 篇文档 · ${item.questions} 道问题</div><details class="answer"><summary>查看许可证原文</summary><pre class="license">${escapeHtml(item.license_text||"仓库根目录未发现 LICENSE/COPYING/NOTICE 文件，请自行查看仓库说明。")}</pre></details></article>`).join("")}catch(error){$("#list").textContent=error.message}
